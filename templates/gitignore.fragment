# Append this to your repo's .gitignore. scripts/install.sh does this for
# you. Do NOT add .secrets.baseline here: it is meant to be committed, so
# detect-secrets has something to diff future scans against.

# MegaLinter is no longer part of this library, but a repo that ran it
# before might still carry these paths locally, so they stay ignored.
megalinter-reports/
mega-linter.log

# pre-commit
pre-commit.log
